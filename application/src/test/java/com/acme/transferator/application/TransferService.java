package com.acme.transferator.application;

public class TransferService {

    public void transferMoney(String sender, String receiver, String amount) {
/*        User sender = persistence.getUser(sender);
        User receiver = persistence.getUser(receiver);
        if (sender.getAccount().getBalance().compareTo(new BigDecimal(amount)) < 0) {
            sender.getAccount().annotateTransferReceived(sender, receiver, amount);
        }

 */
    }
}
